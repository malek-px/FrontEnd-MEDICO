<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medico">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicoMain">
        <activity
            android:name=".MainActivity2"
            android:exported="true" />
        <activity
            android:name=".EspacePatient.PatientHome"
            android:exported="true" />
        <activity
            android:name=".EspaceAssistant.AssistantHome"
            android:exported="true" />
        <activity
            android:name=".AcceuilApp.RegistrationActivity"
            android:exported="true" />
        <activity
            android:name=".AcceuilApp.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".AcceuilApp.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.MedicoSplash">
            //No action bar
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>